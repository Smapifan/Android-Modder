# Keep line endings stable across Windows/Linux to avoid "every file changed" diffs
* text=auto eol=lf

# Windows launcher script should remain CRLF for compatibility
*.bat text eol=crlf

# Binary artifacts
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.keystore binary
