# Keep CI stable: configuration cache currently triggers serialization errors in this project
# (see GitHub Actions failures around DefaultWriteContext.write / BeanPropertyWriter).
# Re-enable only after all custom tasks/plugins are proven configuration-cache compatible.
org.gradle.configuration-cache=false
